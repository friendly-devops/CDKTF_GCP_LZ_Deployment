/**
 * Copyright (c) HashiCorp, Inc.
 * SPDX-License-Identifier: MPL-2.0
 */
import { Language } from "@cdktf/commons";
export type ProviderAddArgs = {
    providers: string[];
    language: Language;
    projectDirectory: string;
    cdktfVersion?: string;
    forceLocal?: boolean;
    dist?: string;
};
export declare function providerAdd({ providers, language, projectDirectory, cdktfVersion, dist, forceLocal, }: ProviderAddArgs): Promise<boolean>;
//# sourceMappingURL=provider-add.d.ts.map