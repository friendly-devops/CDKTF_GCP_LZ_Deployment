/**
 * Class to help with reading `versions.json` file
 * published by `provider get` or `provider add` commands
 */
export declare class LocalProviderVersions {
    private versions;
    private get jsonFilePath();
    private readLocalVersionsJson;
    versionsForAllProviders(): Promise<Record<string, string> | undefined>;
    versionForProvider(providerName: string): Promise<string | null>;
}
//# sourceMappingURL=local-provider-versions.d.ts.map